```bash
gemse env create|export|lock

gemse data fetch|hash|validate

gemse run <pipeline> -c config.yaml -o out/

gemse plot spectrum|residuals|rates ...

gemse docker build|run ...

gemse repro status  # provenance and reproducibility info
```

